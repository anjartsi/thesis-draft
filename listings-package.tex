\usepackage{listings}
\lstdefinelanguage{javascript}{
	keywords={
		break, 
		case, catch, continue, 
		debugger, default, delete, do, 
		else, export,
		finally, for, function, 
		if, in, instanceof, 
		let, 
		new, 
		return, 
		switch, 
		this, throw, try, typeof, 
		var, void, 
		while, with
	},
	morekeywords = {
		template, script, style, scoped, pug,
		data,
		methods,
		computed,
		get, set,
		store							
	},
	morecomment=[l]{//},
	morecomment=[s]{/*}{*/},
	morestring=[b]',
	morestring=[b]",
	sensitive=true
}
\lstset{
	language = javascript,
	frame = single,
	basicstyle = \ttfamily
}

\lstdefinestyle{line-numbers} {
	numbers=left,
	stepnumber = 1
 }